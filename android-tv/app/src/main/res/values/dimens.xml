<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Widen Media3's native track-selection popup. The library default
         (150dp) was tuned for the upstream upstream-only labels — once
         IrisTrackNameProvider suffixes "SDH" / "Forced" the rows get
         clipped at the screen edge on TV. -->
    <dimen name="exo_setting_width">240dp</dimen>
</resources>
