diff --git a/node_modules/mediabunny/.bun-tag-fcb3587c0db22042 b/.bun-tag-fcb3587c0db22042
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/mediabunny.d.ts b/dist/mediabunny.d.ts
index b9cec087d4014ffa0dd7eab15ddd03229d6b3d09..255a3ac20fbec0635a9e5f73995fe0fc7e893c3a 100644
--- a/dist/mediabunny.d.ts
+++ b/dist/mediabunny.d.ts
@@ -149,7 +149,7 @@ export declare type AttachedImage = {
  * @group Codecs
  * @public
  */
-export declare const AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "pcm-s16", "pcm-s16be", "pcm-s24", "pcm-s24be", "pcm-s32", "pcm-s32be", "pcm-f32", "pcm-f32be", "pcm-f64", "pcm-f64be", "pcm-u8", "pcm-s8", "ulaw", "alaw"];
+export declare const AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "dts", "pcm-s16", "pcm-s16be", "pcm-s24", "pcm-s24be", "pcm-s32", "pcm-s32be", "pcm-f32", "pcm-f32be", "pcm-f64", "pcm-f64be", "pcm-u8", "pcm-s8", "ulaw", "alaw"];
 
 /**
  * A sink that retrieves decoded audio samples from an audio track and converts them to `AudioBuffer` instances. This is
@@ -3117,7 +3117,7 @@ export declare type MpegTsOutputFormatOptions = {
  * @group Codecs
  * @public
  */
-export declare const NON_PCM_AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3"];
+export declare const NON_PCM_AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "dts"];
 
 /**
  * This target just discards all incoming data. It is useful for when you need an {@link Output} but extract data from
diff --git a/dist/modules/src/codec.d.ts b/dist/modules/src/codec.d.ts
index 0d60cca0ed37973ae73d17fd621a71789fd9cca4..a9f3f5963bef905c17a0e354007218142f22b770 100644
--- a/dist/modules/src/codec.d.ts
+++ b/dist/modules/src/codec.d.ts
@@ -24,13 +24,13 @@ export declare const PCM_AUDIO_CODECS: readonly ["pcm-s16", "pcm-s16be", "pcm-s2
  * @group Codecs
  * @public
  */
-export declare const NON_PCM_AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3"];
+export declare const NON_PCM_AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "dts"];
 /**
  * List of known audio codecs, ordered by encoding preference.
  * @group Codecs
  * @public
  */
-export declare const AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "pcm-s16", "pcm-s16be", "pcm-s24", "pcm-s24be", "pcm-s32", "pcm-s32be", "pcm-f32", "pcm-f32be", "pcm-f64", "pcm-f64be", "pcm-u8", "pcm-s8", "ulaw", "alaw"];
+export declare const AUDIO_CODECS: readonly ["aac", "opus", "mp3", "vorbis", "flac", "ac3", "eac3", "dts", "pcm-s16", "pcm-s16be", "pcm-s24", "pcm-s24be", "pcm-s32", "pcm-s32be", "pcm-f32", "pcm-f32be", "pcm-f64", "pcm-f64be", "pcm-u8", "pcm-s8", "ulaw", "alaw"];
 /**
  * List of known subtitle codecs, ordered by encoding preference.
  * @group Codecs
diff --git a/dist/modules/src/codec.js b/dist/modules/src/codec.js
index 039b9fc16ab7336e2a8cee553cb0833c72c9341a..39b2cb9000bd444e20cefd4d11c647ddd5462c83 100644
--- a/dist/modules/src/codec.js
+++ b/dist/modules/src/codec.js
@@ -53,6 +53,7 @@ export const NON_PCM_AUDIO_CODECS = [
     'flac',
     'ac3',
     'eac3',
+    'dts',
 ];
 /**
  * List of known audio codecs, ordered by encoding preference.
@@ -422,6 +423,13 @@ export const buildAudioCodecString = (codec, numberOfChannels, sampleRate) => {
     else if (codec === 'eac3') {
         return 'ec-3';
     }
+    else if (codec === 'dts') {
+        // No standard WebCodecs MIME string for DTS — return the
+        // codec name so `AudioDecoder.isConfigSupported` rejects it
+        // and the custom `LibavAudioDecoder` we register elsewhere
+        // picks it up via `registerDecoder`.
+        return 'dts';
+    }
     else if (PCM_AUDIO_CODECS.includes(codec)) {
         return codec;
     }
@@ -466,6 +474,12 @@ export const extractAudioCodecString = (trackInfo) => {
     else if (codec === 'eac3') {
         return 'ec-3';
     }
+    else if (codec === 'dts') {
+        // Mirrors `buildAudioCodecString` above — return the codec
+        // name so the browser-native decodability check fails and
+        // our `LibavAudioDecoder` registration takes over.
+        return 'dts';
+    }
     else if (codec && PCM_AUDIO_CODECS.includes(codec)) {
         return codec;
     }
diff --git a/dist/modules/src/isobmff/isobmff-muxer.js b/dist/modules/src/isobmff/isobmff-muxer.js
index 1a1381839f3cf776cf39186c5c326de20286e670..1a8d5654dba06bf7dd08e41bb5a60de6d0a493fb 100644
--- a/dist/modules/src/isobmff/isobmff-muxer.js
+++ b/dist/modules/src/isobmff/isobmff-muxer.js
@@ -575,13 +575,31 @@ export class IsobmffMuxer extends Muxer {
             // (presentation timestamp & decode order are all you need), but it is a concept in ISOBMFF so we need to
             // model it.
             sample.decodeTimestamp = sortedTimestamps[i];
+            // [iris patch] Open-GOP fix. A new GOP's leading pictures present before
+            // the previous GOP's max PTS, so the sorted-PTS->DTS heuristic above
+            // produces a decode timestamp that moves BACKWARDS across the GOP
+            // boundary, which trips the `assert(delta >= 0)` below. Iris's Tier B
+            // worked around it by DROPPING those frames upstream, but they are decode
+            // references for the open GOP, so dropping them stalls the browser decoder
+            // (readyState stuck, no error). Instead, clamp the decode timestamp to stay
+            // monotonic; the per-sample composition offset emitted in `trun` (signed,
+            // version 1) is recomputed from this decodeTimestamp at write time, so the
+            // presentation timestamp (PTS) is unchanged. This is how ffmpeg muxes
+            // open-GOP HEVC into MP4.
+            if (trackData.lastTimescaleUnits !== null) {
+                const minDecodeTimestamp = trackData.lastTimescaleUnits / trackData.timescale;
+                if (sample.decodeTimestamp < minDecodeTimestamp) {
+                    sample.decodeTimestamp = minDecodeTimestamp;
+                }
+            }
             const sampleCompositionTimeOffset = intoTimescale(sample.timestamp - sample.decodeTimestamp, trackData.timescale);
             const durationInTimescale = intoTimescale(sample.duration, trackData.timescale);
             if (trackData.lastTimescaleUnits !== null) {
                 assert(trackData.lastSample);
                 const timescaleUnits = intoTimescale(sample.decodeTimestamp, trackData.timescale, false);
-                const delta = Math.round(timescaleUnits - trackData.lastTimescaleUnits);
-                assert(delta >= 0);
+                // [iris patch] Clamp (belt-and-suspenders for float drift); the
+                // decodeTimestamp clamp above already keeps this non-negative.
+                const delta = Math.max(0, Math.round(timescaleUnits - trackData.lastTimescaleUnits));
                 trackData.lastTimescaleUnits += delta;
                 trackData.lastSample.timescaleUnitsToNextSample = delta;
                 if (!this.isFragmented) {
diff --git a/dist/modules/src/matroska/ebml.js b/dist/modules/src/matroska/ebml.js
index 50cfea7ec0a82b9360dec35174a2c14789241513..06e73c63d53c1b78d75d150e86f4b09de95261a5 100644
--- a/dist/modules/src/matroska/ebml.js
+++ b/dist/modules/src/matroska/ebml.js
@@ -663,6 +663,7 @@ export const CODEC_STRING_MAP = {
     'flac': 'A_FLAC',
     'ac3': 'A_AC3',
     'eac3': 'A_EAC3',
+    'dts': 'A_DTS',
     'pcm-u8': 'A_PCM/INT/LIT',
     'pcm-s16': 'A_PCM/INT/LIT',
     'pcm-s16be': 'A_PCM/INT/BIG',
diff --git a/dist/modules/src/matroska/matroska-demuxer.js b/dist/modules/src/matroska/matroska-demuxer.js
index cee05b8ce7cf6fc31820e29f4baa04aa427a7e4e..f80446a26ae119a4bb57263e5d59d1a49c748d0d 100644
--- a/dist/modules/src/matroska/matroska-demuxer.js
+++ b/dist/modules/src/matroska/matroska-demuxer.js
@@ -780,6 +780,10 @@ export class MatroskaDemuxer extends Demuxer {
                                 this.currentTrack.info.codec = 'eac3';
                                 this.currentTrack.info.codecDescription = this.currentTrack.codecPrivate;
                             }
+                            else if (codecIdWithoutSuffix === CODEC_STRING_MAP.dts) {
+                                this.currentTrack.info.codec = 'dts';
+                                this.currentTrack.info.codecDescription = this.currentTrack.codecPrivate;
+                            }
                             else if (this.currentTrack.codecId === 'A_PCM/INT/LIT') {
                                 if (this.currentTrack.info.bitDepth === 8) {
                                     this.currentTrack.info.codec = 'pcm-u8';
